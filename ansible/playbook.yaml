---
- name: Install ssh
  hosts: master_node
  tasks:
    - name: Setup ssh connection betwen control node and master node
      import_role:
        name: cdn_ssh

    - name: Set up and install docker engine
      import_role:
        name: cdn_docker
